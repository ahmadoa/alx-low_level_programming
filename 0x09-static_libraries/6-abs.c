#include "main.h"

/**
 * _abs - return absolute value of a number
 * @a: the int we gon calculate it's absolute
 * Return: return absolute number
 */

int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (-a);
	}
}
